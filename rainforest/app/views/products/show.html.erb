<%= link_to "Back to Products", products_url %> | <%= link_to "Edit", edit_product_path(@product) %> | <%= link_to "Delete", delete_product_path(@product), method: :delete, data: {confirm: "Are you sure you want to delete this product?"}  %>



<p><%= @product.title %>, <%= @product.brand %></p>
<%= image_tag @product.url %>